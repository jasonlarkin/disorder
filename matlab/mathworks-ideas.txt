Speed-up animation and visualization:

1) a tool to filter out when NUM_PTS > 1E6

2) improve rendering speed and save to disk speed


Parallel Computing

1) OpenMP or MPI?
http://www.ll.mit.edu/mission/isr/matlabmpi/matlabmpi.html
For one of my projects, I need OpenMP (10-100 GB).

HPC Computing

1) run jobs from any device, combination of github + dropbox.
2) Plotting on any device (PC, Tablet, Smart Phone)

cluster(data(large_data)/code(src,scripts)/plots(small_data)) 
<---> 
all_other_devices(code(src,scripts)/plots)
