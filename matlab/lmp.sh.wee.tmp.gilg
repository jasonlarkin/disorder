#!/bin/bash
cd $PBS_O_WORKDIR
module load openmpi-psm-gcc

RUNPATH=runpath
EXEPATH=/opt/mcgaugheygroup/lammps-30Nov10-owl/src

mpirun -np `cat $PBS_NODEFILE | wc -l` $EXEPATH/lmp_openmpi < $RUNPATH/LMP_TMP 
